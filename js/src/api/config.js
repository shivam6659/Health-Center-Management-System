const config = {
    endpoint: {
        getUserList: "users",
        getSellerList: `8080/api/posts/`,
        sendOtp:"8083/api/users/send-otp",
        logError: "/logging"
    }
}
module.exports = config;
